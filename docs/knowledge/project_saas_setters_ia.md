---
name: Proyecto SaaS Setters IA — estratégico, carrera de fondo
description: Proyecto millonario de Ivan — convertir el setter IA n8n actual en SaaS multi-tenant propio. Estado, decisiones confirmadas, recursos pendientes, ruta del plan.
type: project
originSessionId: 7b0f5ec9-bb00-4536-b202-45abd8ab3dd3
---
Ivan está construyendo un SaaS multi-tenant de setters IA para entrenadores online (nicho principal: fitness/coaches) que reemplaza su infraestructura actual de n8n + Supabase + GHL + WhatsApp. Tiene 3–10 entrenadores activos pagando hoy por el servicio n8n. El setter IA es la puerta de entrada del ecosistema Fyzon.

**Why**: n8n es ineficiente (agente no sigue prompts al pie de la letra, coste tokens alto aun con caching). Hay validación de mercado y Fyzon tiene stack canónico Tipo 2 para reemplazar n8n por código propio con control total del agente.

**How to apply**:
- Este es un proyecto de carrera de fondo, nada de prisas. Validación continua con Ivan antes de avanzar fases. Nunca entregar todo de golpe.
- Ruta del plan maestro: `C:\Users\sotob\.claude\plans\lo-que-te-voy-shimmering-toucan.md`. Todo cambio o decisión nueva se registra ahí en el Log (sección 8).
- Decisiones estratégicas confirmadas 2026-04-19 (D1–D7): shared multi-tenant + RLS Supabase; GHL como backbone CRM vía sub-cuenta por trainer (agentes IA nativos de GHL NO se usan, motor IA propio con Claude Agent SDK); plan GHL Agency Unlimited $297/mes basta para MVP; WhatsApp + Instagram + Facebook desde día 1; setup fee + suscripción mensual; stack Tipo 1 (Next.js panel) + Tipo 2 (motor agente TS/Node + Claude Agent SDK en VPS Contabo).
- Arquitectura de prompt (REVISADA 2026-04-19 tras recibir el Core real): **3 bloques**, no 4 capas. (1) CORE Fyzon v2.0 inmutable con secciones XML `<module_hierarchy>`, `<critical_rules>` (11 reglas), `<pre_message_checks>` (9 checks), fases F0-F7 (no F1-F6), hand-off, pipeline GHL, Bloque 7 RAM. (2) DIRECTIVA DEL ENTRENADOR (coach_v3) siempre presente — gana en tono/lenguaje/criterios. (3) DIRECTIVA DEL NICHO opcional — solo referencia del avatar promedio. Jerarquía de decisión explícita: critical_rules > pre_message_checks > Coach > Nicho > resto. El panel del SaaS ES UN COMPILADOR del coach_v3 (formulario → markdown estructurado del Bloque 2).
- **D14**: estrategia híbrida de código base → clonar patrones estructurales de [1prompt-os de Kadzin](https://github.com/genokadzin/1prompt-os) (MIT, producción) + reescribir lógica diferencial Fyzon. Ahorra 2-3 semanas. Clonado análisis en plan sección 4.22.
- **D15**: **Trigger.dev** para orquestación de jobs asíncronos (como usa Kadzin). Elimina necesidad de queue manager propio.
- **D16**: WhatsApp con **Meta Cloud API oficial** como adapter único en MVP (NO Evolution). Adapter pattern inspirado en [whatsapp-agentkit de Hainrixz](https://github.com/Hainrixz/whatsapp-agentkit) — solo como referencia conceptual, no base (Python single-tenant).
- Fases del plan revisadas: 15-20 semanas totales (antes 19-25) gracias a aprovechar 1prompt-os.
- Scope MVP configurador: identidad, nicho, tono, KB, lead magnets con reglas, escalada a humano, enlace externo de agenda (Calendly/TidyCal), horario actividad, 3 canales. FUERA: agenda nativa OAuth, edición avanzada de fases, FAQ.
- Agenda en MVP: enlace externo del trainer (no OAuth de calendario). Detección de reserva vía webhook GHL cuando contacto pasa a "agendado".
- Recursos. **G1 YA ENTREGADO 2026-04-20**: el prompt del setter actual está desgranado en el Core v3 cargado en Supabase (11 bloques con `tenant_id IS NULL`) — Core base + fases F0-F7 + Bloque 7 RAM + hand-off + pipeline GHL. NO volver a pedirlo. Pendientes: G2 export n8n (útil para ver flow completo y contrato payload ManyChat), G3 URL repo GitHub de referencia, G4 skill n8n→Claude, G5 skill agentes WhatsApp, G6 ejemplos conversación C1/C2/C3 (para Hito 9), G7 automations GHL actuales, G8 métricas actuales, G9 estado infra (WA API, app Meta, ManyChat credentials).
- **Patrón webhook ManyChat confirmado 2026-04-20** (captura Ivan): el flow actual usa bloque "Acciones → Solicitud externa" con URL `https://ddwebhook.fyzon.es/webhook/...` apuntando a n8n. Al migrar, Ivan sustituye esa URL por `https://<motor-fyzon>/webhook/manychat/<tenant_token>`. Triggers del flow actual: "El usuario sigue tu cuenta" (IG follow) y "El usuario envía un mensaje + contiene BIENVENIDA".
- **D19 (2026-04-20) — Flujo respuesta asíncrono con debounce**. El motor responde `200 ack` inmediatamente al webhook de ManyChat, acumula mensajes por subscriber en Redis buffer (25s debounce), genera respuesta cuando cierra el buffer, y envía los 1-4 mensajes vía API ManyChat `sendContent` con `subscriber_id` y `channel`. NO se usa respuesta síncrona (el timeout de ManyChat ~10s no da tiempo a Generator+Judge+Splitter). El siguiente paso del flow ManyChat tras la solicitud externa queda vacío — el motor no retroalimenta el flow, habla directamente al canal. **Requiere credenciales API ManyChat del trainer** en `integration_accounts.credentials` (Hito 8, G9).
- **D20 (2026-04-20) — Coach_v3 del tenant dev**. REEMPLAZADA por D21.
- **D21 (2026-04-20) — Trainer nuevo "Pablo Montenegro / Montefit" como tenant de arranque**. En vez de clonar un trainer n8n actual, Ivan entrega coach propio del trainer Pablo Montenegro (Montefit, fitness hombres 30-50 ocupados, acento venezolano). Se carga como tenant_id=2, slug='montefit', con `coach_v3` único en un bloque (sort_order=5). Fuente: `prompts/source/coach-v3/pablo-montenegro.md` (fiel al entregado por Ivan). Script generador: `scripts/build-coach-seed.mjs`. Seeds: `003-tenant-montefit.sql` + `004-coach-pablo-montenegro.sql`. Feedback al coach (11 puntos — banco<20 frases, F3/F4 sin mensajes obligatorios, numeración fases coach vs core, regla del tercio estricta, URL agenda pendiente, etc.) guardado en `prompts/source/coach-v3/pablo-montenegro.feedback.md` para iterar v2 tras pruebas C1/C2/C3.
- **D22 (2026-04-20) — URL de agenda ENTREGADA**. Ivan pasó `https://cal.com/ivan.soto/consultoria` (Cal.com, no Calendly). Cargado en coach_v3 v1.1 (18.951 chars). ⚠️ es la URL propia de Ivan, no de Pablo — intencional para pruebas internas. Sustituir por la de Pablo cuando vaya a producción real.
- **D23 (2026-04-20) — Credenciales ManyChat montadas**. API key Ivan entregada y cargada en `integration_accounts` (tenant_id=2, channel WhatsApp/manychat, page_id=3738205). Formato ManyChat es `page_id:secret` (3738205:[REDACTADO — valor real en integration_accounts.credentials_encrypted, tenant 2]). Cubre WA/IG/FB del bot ManyChat. Credenciales encriptadas at-rest en Supabase.
- **Para pegar en ManyChat** (bloque "Acciones → Solicitud externa"): URL = `https://<host-motor>/webhook/manychat/[REDACTADO — tenant_tokens, tenant 2, purpose=manychat_webhook]`. En producción se sustituye `<host-motor>` por el dominio del VPS Contabo. En dev (con ngrok/cloudflared): la URL del túnel.
- **D24 (2026-04-21) — Contrato ManyChat validado con payload real via "Probar Solicitud"**. Campos variables ManyChat usados: `{{Id de contacto}}` (subscriber id numérico stringificado, p.ej. "1348224655"), `{{Nombre}}`, `{{Apellido}}`, `{{Teléfono}}`, `{{Última entrada de texto}}`. Timestamp NO incluido (se genera server-side). `origin_trigger` custom string propio. El parser Zod acepta todo correctamente. Canales instagram_dm y whatsapp ambos registrados con integration_account ManyChat (misma API key cubre page_id 3738205).
- **D25 (2026-04-21) — Sanitizer de placeholders en parser**. En IG DM ManyChat no puede interpolar `{{phone}}` (IG no expone teléfono), llega literal. Añadido `stripUnresolvedPlaceholder` en `packages/channel-adapters/src/manychat/types.ts` que convierte `"{{xxx}}"` → null antes del upsert. Cubre phone/email/username/first_name/last_name/locale/gender. Test unitario añadido. 8/8 tests pasando.
- **Trigger real no disparó en sub-fase 2026-04-21**: Ivan envió `TESTFYZON` a IG de Pablo pero el flow con palabra clave no capturó. No es bloqueante — contrato validado via "Probar Solicitud". Hipótesis: (a) ventana 24h Meta IG DM, (b) catch-all con prioridad, (c) match config. Investigar en Hito 8 cuando bot responda en vivo.
- **D26 (2026-04-21) — Arquitectura del prompt composer** (`packages/prompt-composer`): función pura `buildComposedPrompt(rows, options)` + wrapper `composePrompt(supabase, options)`. Orden fijo: `core_v3_base` → `coach_v3` → `fase_<N>_v3` → cualif/handoff/pipeline (opt) → `objeciones_v3`. Query única a `prompt_blocks` con `(tenant_id IS NULL OR tenant_id=$1) AND is_active AND version=1`. En duplicados, prefiere scope tenant sobre shared. Cache strategy default `'two-point'`: breakpoint en core_v3_base y último bloque del prefix → 2 cache hits por llamada de Anthropic. Alternativas: `'single-point'` y `'none'`. Hard fail si faltan `core_v3_base` o `coach_v3` o la fase activa. CLI `preview-prompt` para debug visual contra DB live.
- **D27 (2026-04-26) — Generator architecture** (`packages/agent-pipeline`): `runGenerator(deps, input)` recibe `{supabase, anthropic}` y `{tenantId, conversationId, userMessage, currentPhase, history, model?, maxTokens?, composeOverrides?}`. Tool `respond_as_setter` con `tool_choice` forzado garantiza output estructurado. Schema valida `message_raw` (1-1500 chars), `conversation_status` (active/qualified/disqualified/handoff/paused), `phase_decision` (1..7), opcionales user_summary/resources_to_send/handoff_cause/reasoning. Cost calculator separado en `cost.ts` con tabla por modelo (Sonnet/Haiku/Opus 4.5) y heurística de prefix para snapshots. Best-effort log a `llm_calls` con request_payload (system_chars, blocks, cache_breakpoints, messages_count) y response_payload (stop_reason + summary del tool input). Validate output no propaga si hay fields opcionales inválidos (drop silencioso) — mantiene robustez ante variaciones del modelo.
- **D28 (2026-04-26) — Pricing Sonnet 4.5 confirmado en producción**: $3/M input uncached, $0.30/M cache_read, $3.75/M cache_write, $15/M output. Coste real medido turno 1: $0.085 (21.8k tokens cache write + 233 out). Turno 2: $0.047 (11.5k cache read + 10.2k cache write parcial por cambio de fase). Para conversación 14-20 turnos estimo $0.7-1.5 por lead (50-70% del prefix se reutiliza). Viable a escala SaaS con setup fee + suscripción mensual.
- **D29 (2026-04-27) — Coach Pablo v2 PARADA hasta tener G6 + dump `llm_calls` reales**. Tras revisar los 11 puntos del feedback con el motor cerrado (Hitos 3-8 ✅, 110/110 tests verde) Ivan elige NO iterar v2 sin evidencia empírica (las 3 conversaciones canónicas C1/C2/C3 + dump de la tabla `llm_calls` con corridas reales tenant_id=2). Iterar el coach por intuición sin baseline contaminaría la decisión: si después algo sale raro en C1/C2/C3 no sabremos si fue v2 o el coach v1.1 lo que falló. **Decisiones de coach cerradas hoy** (a aplicar cuando v2 arranque): **P2** F3/F4 hereda del Core sin overrides específicos (las preguntas prohibidas del avatar ya cubren transversalmente); **P6** D9 "fuera de rango de avatar" condicional (solo activa si concurren edad 18-29 + perfil claramente alejado del avatar; jóvenes que sí encajan cualifican normal). Plan completo: `~/.claude/plans/this-session-is-being-snazzy-puddle.md`. Decisiones P2/P6 documentadas en `prompts/source/coach-v3/pablo-montenegro.feedback.md` sección "Decisiones cerradas pre-v2". El coach activo en Supabase sigue siendo v1.1 (≈18.961 chars). **Para arrancar v2**: Ivan tiene que entregar G6 (3 .md de conversaciones 10/10 en `prompts/fixtures/C1.md`/`C2.md`/`C3.md` con perfil + turnos LEAD/BOT + outcome) + el smoke E2E real Hito 8 sirve como fuente de evidencia adicional si lo ejecuta antes.
- **D30 (2026-05-0X) — YCloud incorporado como segundo proveedor BSP de WhatsApp en paralelo a ManyChat**. WA primario para tenants nuevos: YCloud es BSP oficial Meta con autorización directa, mejor que ManyChat como pasarela. ManyChat sigue cubriendo IG/FB y tenants legacy hasta migración a GHL. Adapter completo en `packages/channel-adapters/src/ycloud/{api-client,parser,types,whatsapp}.ts`. Multi-provider routing en `apps/motor-agente/src/services/outbound-sender.ts:buildAdapter()`. Ruta nueva `/webhook/ycloud/:token` con dedup por `wamid`. Migration `005-ycloud-provider-enum.sql` aplicada en Supabase (enum `channel_provider` ahora tiene `manychat, meta_cloud, ghl, other, ycloud`). Patrón documentado en `CLAUDE.md` del repo (sección "Multi-provider routing").
- **D31 (2026-05-0X) — Tenant `ivan-dev` (id=3) "Ivan / Fyzon Sandbox"** creado como entorno propio de pruebas para validar el motor end-to-end usando el WhatsApp Business real de Iván vía YCloud, antes de tocar Pablo/Montefit u otros clientes. Coach v3 real escrito en `prompts/source/coach-v3/ivan-dev.md` (Iván Soto / Fyzon, 18.808 chars), no placeholder. Seeds 005 (tenant + config + canal WA via YCloud + token webhook + integration_account) + 006 (coach_v3) aplicados. Webhook URL: `https://<motor>/webhook/ycloud/[REDACTADO — tenant_tokens, tenant 3, purpose=ycloud_webhook]`. Business phone YCloud: `+34684703803`.
- **D32 (2026-05-06) — Smoke E2E pendiente del Hito 8 (Pablo/montefit vía ManyChat IG) se difiere indefinidamente**. El contrato ManyChat ya fue validado en sub-fase 2026-04-21 con payload real. Validar la respuesta visual no aporta información nueva cuando YCloud + ivan-dev cubren la prueba con coach + número propios de Ivan. Pablo se reactivará cuando haga falta o pasará por GHL en hito posterior.
- **D33 (2026-05-06) — Tras smoke YCloud OK, prioridad = Hardening pre-producción** (encriptación AES-256-GCM de credentials + HMAC verification webhooks + observabilidad mínima con correlation IDs y tabla `pipeline_runs`). NO Hito 9 GHL sync, NO Fase 2 Panel, NO G6/coach Pablo v2 todavía. Cierra el riesgo "credenciales de cliente en plain text en Supabase" antes de cualquier cliente real. Plan detallado: `~/.claude/plans/planifica-como-podemos-seguir-quiet-wigderson.md`.
- **D34–D40 (2026-05-06) — Reescritura Core v3 → v4** post reunión equipo Fyzon (2h12m). Formato XML modular en `prompts/source/core-v4/` (11 archivos + README), seed `007-core-v4-blocks.sql`, migration `008-deactivate-v3-activate-v4`. **Co-redacción Iván↔Claude sección por sección** (23 secciones, OK explícito por sección). Decisiones: TEMA PRINCIPAL ÚNICO sustituye D-vs-P (D35), validación emocional sin "te entiendo" literal (D36), aceptar tamaño actual ~12k tokens — economía vendrá de menos turnos no de prompt corto (D38), quitar `<identity_as_core>`+`<protocolo_pipeline_GHL>`+ejemplos genéricos+límite extensión+reflejos/mirror+atención manual+cualificación general+introducciones variadas (mover al Coach), 5 elementos por fase, conectores XML. Plan detallado: `~/.claude/plans/c-users-sotob-downloads-prompt-ejemplo-quirky-puffin.md`. Smoke YCloud + commits + Hardening DIFERIDOS a post-v4 estable.
- **D41 (2026-05-07) — Eliminar bloque NICHO completo**. Arquitectura pasa de Core+Coach+Nicho a **Core+Coach únicamente**. El avatar tipo del nicho de mercado se absorbe dentro del Coach del trainer. Razón: complejidad sin valor proporcional con 1 trainer por nicho operativo. Reabrir si 2+ trainers comparten nicho exacto. `<module_hierarchy>` queda absorbido dentro de `<role>`. Sección 1 del Core v4 (`<role>`) ya firmada por Iván con esta decisión incorporada (`prompts/source/core-v4/01-role.md`).
- **D42 (2026-05-07) — Core agnóstico al estilo**. El Core describe estructura y propósito; el Coach describe wording, aperturas, repertorio de validaciones, emojis y registro. El Core NO incluye ejemplos concretos de aperturas/palabras/muletillas/emojis — condicionarían a trainers con estilos distintos. Las reglas universales sobre el "cómo" (ej: prohibido empezar con "Y…", no decir "te entiendo" literal, no repetir misma apertura 2 turnos) sí van al Core porque son válidas para todo trainer; los ejemplos del "cómo SÍ se dice" son responsabilidad del Coach.
- **D53 (2026-05-08) — Smoke v4 E2E real OK + Hito 9 cerrado en código**. Iván envió WhatsApp real al business `+34684703803` via cloudflared tunnel + endpoint YCloud configurado. 10 turnos completados, conversation_id=6, lead `+34639541043`. **Costes pipeline_runs medidos**: turn 1 cold $0.0438 (cache_write 1h Haiku 4.5), turns 2-N warm $0.005-0.007. Proyección 14-turn convo: **0.92¢/mensaje** ✅ debajo del objetivo 1¢ del Paso 3.5. HMAC verify_mode=`warn` registró `signature_mismatch` en logs sin romper (esperado: el `webhook_secret` en DB no coincide con el `whsec_3c63...` real de YCloud todavía). correlation_id UUID propagado a logs y `pipeline_runs` (1 fila por turno con métricas detalladas por etapa). Hito 9 ✅ cerrado en código (Hardening 1.1+1.2+1.3 todos commiteados + push a `origin/main`).
- **D54 (2026-05-08) — Iteración Cerebro v3 → v4.1 con feedback empírico real**. Tras 4 turnos del smoke, Iván detectó 3 fricciones: (a) muletillas afirmativas repetidas turno tras turno ("Claro/Vale, perfecto/Entiendo/Claro"), (b) em-dash `—` apareciendo en 2/4 turnos AI (señal típica de IA), (c) **asunción no verbalizada** crítica — el bot dijo "eso es común cuando crecéis rápido" sin que el lead hubiera mencionado crecimiento. Decisión: 3 reglas universales nuevas en `prompts/source/core-v4/01-role.md`: **R16 Anti-asunción** (Crítica, prohíbe inferir, generalizar a partir de patrones, presumir contexto no verbalizado — universal a entrenadores/empresas/cualquier nicho), **R17 Anti-em-dash** (Crítica, prohíbe `—` en mensajes; sustituir por coma o reformular; única excepción mirroring si el lead lo usa), **RC7 Variación de muletillas** (Condicional, prohíbe repetir misma muletilla afirmativa en turnos consecutivos). Jerarquía actualizada R1-R17 + RC1-RC7. core_v4_base subió 16807 → 19297 chars. Aplicado vía execute_sql 2026-05-08 06:29 UTC.
- **D58 (2026-05-08, sesión GHL-as-channel) — Hito 10 revisado: GHL como canal nativo IG/FB, smoke 1 OK end-to-end**. Branch `feat/ghl-as-channel` con 7 commits + push (`bffe260..cbba76c`). Plan vigente: `~/.claude/plans/planifica-los-pr-ximos-pasos-fancy-puddle.md`. **Cambios arquitectónicos** (descarte parcial del plan B original): GHL ya NO es destino de replicación posterior — es el canal nativo de inbound + outbound IG/FB para tenants nuevos. Pipelines/opportunities en GHL **descartados** definitivamente; el visual `fuente × fase` se construirá en Panel SaaS Fase 2 leyendo `conversations.conversation_source` + `phase_number`. WhatsApp sigue YCloud por tenant (no migra). **Implementación**: migrations 012 (`message_source += 'human'`) + 013 (`conversation_source` + `ai_paused_until` + tabla `automation_keywords`); `packages/channel-adapters/src/ghl/{parser,channel-adapter}.ts` con parser que detecta DOS formatos (Marketplace App con `type='InboundMessage'` + RSA signature; Workflow Webhook con `contact_id`/`location.id`/`message.{type:int,body}` snake_case + sin firma — PIT no firma); `packages/ghl-client/src/messages.ts` extendido con `sendMessageViaChannel` + `appendZwspIfMissing` (ZWSP idempotente); `apps/motor-agente/src/lib/webhook-verify-ghl.ts` con verifier RSA-SHA256 (modos `disabled|warn|enforce`, default `disabled` para PIT); endpoint `POST /webhook/ghl/:tenant_token` + `GET /webhook/ghl/:tenant_token/ping` (test connectivity); `services/ghl-message-router.ts` con `routeGhlInbound` + `routeGhlOutbound` + `classifyByKeywords` (precedencia bienvenida>lm>inbound, case-insensitive, ZWSP detection, pause IA via `ai_paused_until='infinity'` cuando humano escribe sin ZWSP); gate `ai_paused_until` en `process-debounced.ts` antes de `runPipeline`; refactor `outbound-sender.ts` con `SupportedProvider += 'ghl'` + `case 'ghl' → GhlChannelAdapter`; **borrado** `ghl-sync.ts` + 14 tests (lógica obsoleta de opportunities/replicación). Tests: 263 verde (parser-ghl 20, sendMessageViaChannel 7, verifier-ghl 7, classifyByKeywords 8). **Seed tenant 3**: nuevo channel id=5 (instagram_dm via ghl), integration_account id=4 (provider='ghl', PIT cifrado AES-256-GCM en `credentials_encrypted`, `connection_config={"locationId":"FOxJtkxqNKJjGSuYMEk0"}`), tenant_token id=3 (purpose='ghl_webhook', token=`[REDACTADO — tenant_tokens, tenant 3, purpose=ghl_webhook]`). Iván tiene Private Integration Token `pit-44dfa08a-...` para sub-cuenta Fyzon real (NO sandbox aparte — Iván confirmó OK meter pruebas en producción). **Setup tunnel cloudflared**: subdominio fijo `setter-dev.fyzon.es` (CNAME en zona Cloudflare Iván) → tunnel id `55097125-2ced-43f7-9a3c-586dc660d914` → `localhost:3001`. **Smoke 1 real** (2026-05-08 10:25-10:31 UTC) Workflow GHL "Flujo IA 1" trigger Customer Replied IG → 3 turnos lead↔ai exitosos, 3/3 pipelines OK, judge 1×fix + 2×pass, splitter 1 parte/turno, **costes**: turn 1 cold $0.046 (cache write 1h), turns 2-3 warm $0.007 cada uno → proyección 14-turn = **0.96¢/msg** ✅ debajo objetivo 1¢. Conversación natural, R17 anti-em-dash respetado, estilo coach Iván/Fyzon. **Pendientes Bloque C** (no bloqueantes): (a) Workflow B Outbound (detección humano + pausa IA via ZWSP) sin configurar/validar todavía; (b) formato Workflow custom con `customData` (`lead`, `conversation_source`, `message`) del flow legacy keyword "clase" no probado; (c) tabla `automation_keywords` creada vacía — Iván cargará patrones bienvenida/lm/inbound cuando los tenga; (d) merge `feat/ghl-as-channel` → `main` cuando smoke 2 también OK. **Lecciones operativas**: (1) Workflow webhook GHL envía formato distinto al Marketplace API — `contact_id` snake_case, `location` nested, `message.type` integer (18=IG, 5=FB, 7=WA, 1=SMS, 2=Email). Parser dual fue la solución; (2) PIT (Private Integration Token) NO firma webhooks (eso es de Marketplace Apps); con PIT, autenticación = token random en URL + opcional Authorization Bearer custom header en Workflow; (3) cloudflared tunnel named con subdominio Cloudflare propio = URL fija sin pagar nada; (4) `GET /ping` endpoint paralelo = útil para validar conectividad antes de tirar pipeline. **Ruta inmediata** próxima sesión: configurar Workflow B (Outbound trigger Customer Replied → webhook), cargar automation_keywords reales, validar smoke 2 (pausa IA), después merge a main + decision gate Fase 2 Panel SaaS vs onboarding Pablo real.

- **D58.10 (2026-05-08, sesión vista de pájaro) — Análisis 6 sombreros + 6 expertos + 3 ejes (Meta API, GHL post-Meta, BYOK panel) + Fase 2 MVP2 detalle + shadcn rediseño con sidebar shell**. Iván pidió revisión profunda en momento crítico antes de decisiones grandes (Meta API oficial llegó esta tarde). **Sesión secuencia**: (a) Fase 2 MVP2 detalle conversación commit `6a29aa3` con timeline chat-style + media inline. (b) Iván dijo "está OK pero V0 visual" → migración shadcn/ui (init Nova preset Lucide+Geist + 14 componentes + route group `(app)/` + AppSidebar colapsable + dashboard 4 KPIs + lista shadcn Table + detalle Card + sonner toasts) commits `caa51ad`+`151fb9b`+`2dec71b`. (c) Solicitud vista de pájaro con 6 sombreros sobre 3 ejes críticos. **Decisiones consolidadas plan vigente**: (1) **Eje 1 — Meta API directa**: NO migrar ahora. Mantener GHL como gateway durante validación cliente real. Motor multi-provider ya soporta añadir Meta como provider más en futuro (~10-15h código, no rewrite). Riesgos negros identificados: 30-50h migración + shiny new toy syndrome + perder funciones CRM GHL + sin fallback si Meta cambia API. Beneficios amarillos: control total + coste cero CRM + latencia menor. Verde: solución híbrida Meta canal + GHL CRM. (2) **Eje 2 — GHL post-Meta**: si pasa a Meta directa, GHL queda como CRM puro vía sync async best-effort (recuperar `ghl-sync.ts` borrado en C.5 desde git history). Coste 5-7h adicional. (3) **Eje 3 — BYOK panel**: NO es humo, es estándar SaaS. NO via MCP (eso es para Claude Desktop, no para clientes web finales). Patrón: form en `/settings/integrations` con dropdown provider + credenciales → Server Action valida (ping API) + cifra AES-GCM + UPSERT integration_accounts. Motor ya soporta multi-provider en runtime, sin recompilar. ETA 3-5h código nuevo (UI panel). **Lo que NO debemos hacer**: (a) empezar Meta API antes de cerrar Bloque C → branch eterno. (b) quitar GHL antes de validar cliente real. (c) construir CRM propio panel → alcance infinito. (d) rebase agresivo historial commits "backup" pre-merge. **Acciones para Iván**: reunión Meta esta tarde con preguntas (¿permisos directos? ¿scopes aprobados? ¿WA Business número verificado? ¿timeline?). **Mientras**: avanzar MVP2.5 settings/integrations + MVP2.6 keywords UI (independientes de Meta vs GHL). **Estado branch `feat/ghl-as-channel`**: 17 commits push (último `2dec71b`). 162 motor + 54 ch-adapters + 27 ghl-client = 243 tests verde. Plan vigente actualizado con vista de pájaro completa.

- **D58.9 (2026-05-08, sesión continuación) — Fase 2 Panel SaaS MVP1: lista conversaciones + botón pausar/reactivar IA**. Tras cerrar Bloque D multimodal con smoke audio+imagen OK, Iván decidió saltar a Fase 2 mientras GHL revisa la app Marketplace. **Implementación MVP1** (~3-4h, commit `caa51ad` push): (a) `apps/panel/lib/actions/conversations.ts` Server Action `togglePauseConversation(conversationId, currentlyPaused)`: pausa→ai_paused_until='infinity' / reactiva→NULL; doble check tenant_id en BD + RLS refuerzo; revalidatePath('/conversations'). (b) `apps/panel/app/conversations/page.tsx` server component: auth + profile lookup + query conversations JOIN leads+channels (limit 100, order updated_at DESC), maneja 3 estados (sin tenant / vacío / con datos), nav tabs hacia dashboard. (c) `apps/panel/app/conversations/conversations-table.tsx` client component: tabla con 9 columnas (ID, lead, canal, fase, origen, estado, IA running/paused, último, acción), `StateBadge` con priorización handoff>qualified>state, `isAiPaused()` detecta string "infinity" + timestamps futuros, `PauseToggleButton` con useTransition + error inline, `formatRelative` para timestamps. (d) `middleware.ts`: añade `/conversations` a PROTECTED_PREFIXES. (e) `app/dashboard/page.tsx` + `app/page.tsx`: añadir `dynamic='force-dynamic'` para evitar prerender error sin env vars en build. (f) `globals.css`: ~250 líneas nuevas — `.dashboard-nav`/`.dashboard-nav-link.is-active`, `.conversations-table` con scroll horizontal, badges color-coded (channel=azul, phase=ámbar, source=verde/violeta/azul/rosa según bienvenida/lm/inbound/manual, state, qualified, handoff, paused/active IA), botones pause (ámbar) / resume (verde primary). **DB cambio**: `UPDATE profiles SET tenant_id=3 WHERE id=<Iván>` (estaba en tenant 1 dev placeholder, ahora alineado con todas las conversaciones reales en tenant 3 Fyzon). **Failsafe Path A confirmado**: el botón complementa la pausa primaria que vendrá de la App Marketplace (cuando GHL apruebe versión draft). El motor ya tiene gate `ai_paused_until` en `process-debounced.ts:1.5` y `routeGhlInbound:6`, así que el botón solo escribe en BD y los gates ya respetan. **Build**: `pnpm --filter @fyzon/panel build` OK + typecheck monorepo verde. **Pendiente smoke manual** Iván: `pnpm --filter @fyzon/panel dev` (puerto 3000) → login con sotobautistaivan@gmail.com → /conversations → ver lista 8+ conversaciones tenant 3 + click "Pausar IA" en una activa → BD UPDATE ai_paused_until='infinity' + revalidate → row pasa a estado pausada. Si funciona, MVP2 sigue: detalle conversación + automation_keywords UI configurador (~5-7h adicionales). Listo para merge a main tras smoke manual.

- **D58.8 (2026-05-08, sesión continuación) — Bloque D Multimodal IA (Groq Whisper audio + Claude Sonnet 4.5 vision imagen)**. Promovido desde "diferido" a bloque activo en paralelo mientras GHL revisa la app Marketplace para publicar versión (estado app=draft bloquea install). Iván decidió: Groq Whisper-large-v3-turbo (~$0.04/hora vs $0.36 OpenAI) + Claude vision nativa + ES default per-tenant + EN auto-fallback. Cambios: (a) Migration 014 aplicada via MCP: `tenant_configs.default_audio_language` ('es'|'en'|'auto', default 'es') + `pipeline_runs.multimodal_audio_seconds, multimodal_image_count, multimodal_cost_usd`. Tipos regenerados manualmente (CLI supabase no instalada local). (b) `lib/transcribe-audio.ts` cliente Groq: download buffer (timeout 30s, max 25MB) → POST multipart `/audio/transcriptions` con model+language+response_format=verbose_json → parse {text,language,duration}, cost=duration/3600*0.04. `TranscribeAudioError` con 6 reasons clasificados. (c) `lib/describe-image.ts` Claude vision: download + base64 + detect media_type por content-type o extensión, soporta jpg/png/gif/webp, llama Anthropic SDK multimodal (Sonnet 4.5, max 500 tokens out, prompt fijo "describe brevemente para agente IA"), coste via `calculateCostUsd` reusado de `@fyzon/agent-pipeline`. `DescribeImageError` con 6 reasons. (d) `services/enrich-media-messages.ts` orquestador: query rows lead con content=NULL+media_url no-null (limit 10) → audio→transcribe / image→describe / video,file→placeholder → UPDATE content="[Audio del lead]: <text>" o "[Imagen del lead]: <text>" + transcription=raw. Best-effort: si falla, persiste placeholder legible + continúa. Devuelve métricas agregadas. (e) `process-debounced.ts` step 3.7 nuevo: `loadAudioLanguage(tenantId)` + `enrichMediaMessages(...)` ANTES de `loadConversationHistory` → cuando carga history, content ya está poblado, pipeline arranca normal. mediaResult.costUsd se suma a totalCostUsd retornado. (f) `pipeline-runs.ts:completePipelineRun` acepta opcional `multimodal: {audioSecondsTotal, imageCount, costUsd}` y persiste en pipeline_runs.multimodal_*. Tests: +18 nuevos (10 transcribe-audio mock fetch+groq todos los reasons + 8 describe-image mock anthropic+download). **Total: 161 motor + 54 channel-adapters + 27 ghl-client = 242 verde**, typecheck monorepo verde. Commit `d655b65` push. **Pendiente smoke D.7**: Iván registra cuenta `console.groq.com` (free tier generosa, 6000 RPM whisper) → API key → añade `GROQ_API_KEY=...` a `.env.local` → envía audio IG (~10s, ES) + imagen IG → BD muestra rows con content="[Audio]: ..." y "[Imagen]: ..." + `pipeline_runs.multimodal_cost_usd` ≤ 0.5¢ típico. **Riesgo conocido**: URLs media GHL pueden requerir auth Bearer apiToken; si descarga falla con 401/403, añadir Authorization header en `transcribe-audio` + `describe-image` con el access_token OAuth (cuando esté). **Coste objetivo**: ~1¢/msg si hay multimedia (vs 0.92¢ texto-only). **Multimodal en pipeline propio NO se modifica**: se hace pre-procesamiento que entrega texto al Generator existente (no hay multimodal en Generator/Judge/Splitter). **Smoke D.7 audio CONFIRMADO ✅ 2026-05-08 20:13** tras fix `.mp4 → audio` (commit `fee250f`): IG DM voice notes vienen como `.mp4` (contenedor MPEG-4 con codec audio AAC) y la heurística inicial los mapeaba a `video` → enrich saltaba con placeholder "[Video recibido]" → Generator respondía como si fuera video. Fix: `.mp4` ahora se trata como `audio`; Whisper procesa el track de audio igual aunque fuera video real (raro <5%). Tras fix + reinicio motor, Iván envió audio 5s ES → Whisper transcribió correctamente → IA respondió al contenido real del audio. **Lección operativa para futuros canales media**: las extensiones de archivo no son fiables para distinguir audio vs video (mp4/webm/mov son contenedores ambiguos); IG en particular usa mp4 universalmente para voice notes. Si en el futuro Iván recibe muchos videos reales, añadir detección por Content-Type del download HTTP. **Smoke D.7 imagen CONFIRMADO ✅ 2026-05-08 20:23** (run id=20): Iván envió foto IG (oficina caótica con hombre estresado + pantallas + documento "Onboarding checklist") → Claude Sonnet 4.5 vision generó descripción precisa identificando todos los detalles relevantes en español → splitter dividió respuesta IA en 3 partes coherentes que conectaban audio previo ("4 personas") + imagen ("ofi caótica") + texto ("Así estamos siempre"). **Coste imagen: $0.0048 (0.48¢, 1500 tokens input)**. Total turno con imagen: $0.0161 (1.6¢) — ligeramente sobre proyección 1¢ pero aceptable. **Bloque D cerrado end-to-end con 2 modalidades validadas en producción real**. Costes reales medidos: turno texto warm ~0.7¢, +audio Whisper ~0.71¢ (Groq es absurdamente barato), +imagen Claude vision ~1.6¢. 162/162 motor + 54 ch-adapters tests verde tras fix `.mp4 → audio`.

- **D58.7 (2026-05-08, sesión continuación) — C.E.0/2/3/4 App Marketplace GHL propia con OAuth + webhook firmado**. Iván confirmó que la app GHL legacy (`698900ff70e859516bab39dd-mleb66zl`, creada por dev de proyecto-automatiapro-n8n) NO se puede reconfigurar — Iván solo tiene acceso de instalación, no permisos dev. Decisión: **crear app Marketplace propia** (Distribution Sub-Account, privada). Iván ya creó la app + configuró URLs. Pendiente que pase credentials. **Bloqueo previo encontrado**: developer portal GHL valida y rechaza redirect URIs / webhook URLs que contengan "ghl"/"highlevel" — error literal "The redirect uri contains a Highlevel reference. Please remove any Highlevel references to save." → C.E.0 rename `/webhook/ghl/*` → `/integrations/webhook/*` (commit `48267dd`). **Implementación C.E completa** (commit `f998069`): (a) `apps/motor-agente/src/lib/ghl-oauth.ts` con `exchangeCodeForTokens` (POST /oauth/token grant_type=authorization_code form-urlencoded), `refreshAccessToken` (grant_type=refresh_token), `buildOauthIntegrationFields` (encrypt AES-GCM blob + connection_config plain), `getValidAccessToken` (lee row, comprueba expiry, refresh automático <5min, persiste tokens nuevos). `GhlOauthError` con status + bodySnippet. (b) `apps/motor-agente/src/lib/webhook-verify-marketplace.ts` verifier dual: detecta header `x-wh-signature` (RSA, delega a verifier existente) o `x-ghl-signature` (HMAC-SHA256 con shared secret); preferencia RSA > HMAC si ambos. HMAC parser soporta formato `t=<unix>,s=<hex>` o `<hex>` raw; tolerance anti-replay 300s default; timingSafeEqual. (c) `apps/motor-agente/src/routes/oauth-ghl.ts`: `GET /integrations/oauth/install?tenant_token=X` genera state UUID Redis TTL 600s + 302 redirect a `chooselocation`. `GET /integrations/oauth/callback?code=X&locationId=Y&state=Z` decodifica state (single-use), exchange code → tokens, UPSERT integration_accounts (re-install detectado por tenant+locationId+oauth), HTML render confirmando. (d) `apps/motor-agente/src/routes/webhook-ghl.ts` handler nuevo `POST /integrations/webhook/oauth` (sin tenant_token URL): resolveTenantByOauthLocation lookups `connection_config.auth_type='oauth' AND connection_config.locationId=<X>` → tenant_id. Si no found → 200 ack ignored. Reutiliza parser dual + routeGhlInbound/Outbound. Dedup prefix `ghl_oauth:` separado. (e) Env vars OAuth en `config/env.ts`: `GHL_OAUTH_CLIENT_ID`, `GHL_OAUTH_CLIENT_SECRET`, `GHL_OAUTH_SHARED_SECRET`, `GHL_OAUTH_REDIRECT_URI`, `GHL_OAUTH_SCOPES`, `GHL_API_BASE`, `GHL_MARKETPLACE_BASE`. (f) Plugin registrado en `server.ts`. **Esquema DB**: reusar `provider='ghl'` en integration_accounts, diferenciar `connection_config.auth_type='oauth'` vs `'pit'`. Sin migration nueva. **Persistencia OAuth**: `credentials_encrypted = AES-GCM(JSON({accessToken, refreshToken}))`, `connection_config = {auth_type:'oauth', locationId, companyId, scope, userType, expiresAt, installedAt}`. Tests: +11 verifier marketplace + 9 oauth lib = 20 nuevos. **143 motor-agente** + 54 channel-adapters + 27 ghl-client = 224 verde. Typecheck monorepo verde. **Hooks observación**: el sistema auto-backup de Iván captura mis cambios con mensaje genérico "backup: pre-deploy safety checkpoint" antes de que pueda commitear con mensaje descriptivo. Commits C.F=`e9a7864`, C.E.0=`48267dd`, C.E.2-4=`f998069` todos con ese mensaje. Contenido correcto, mensajes pueden enriquecerse con interactive rebase pre-merge si se quiere historial limpio. **Pendientes Iván**: (1) pasar Client ID + Client Secret + Shared Secret + Install URL para meter en `.env.local`. (2) Generar Shared Secret en developer portal (botón "Generate key"). (3) Actualizar Workflow `Flujo IA 1` con URL `/integrations/webhook/<token>` (rename C.E.0). (4) Smokes G/H/I/CE en sesión real cuando vuelva.

- **D58.6 (2026-05-08, sesión continuación) — C.F persistencia attachments inbound (audio/imagen/video/file)**. Iván añadió `attachments={{message.attachments}}` al webhook step del Workflow GHL (Flujo IA 1) para que los audios/imágenes que el lead manda lleguen al motor. Cambios: (a) `parseAttachmentsRaw(raw)` helper en `parser.ts` normaliza array | JSON string | CSV | empty/null/false a `string[]` con dedup + trim + drop non-strings. `workflowSchema` acepta `attachments` top-level + customData.attachments; `normalizeWorkflowToInternal` los puebla en `payload.attachments`. (b) `routeGhlInbound` ahora inserta 1 row text (si hay) + 1 row por adjunto en `conversation_messages` con `media_url` + `content_type` inferido. Helper `inferContentTypeFromUrl()` mapea por extensión: mp3/ogg/wav/m4a/aac/opus → audio; jpg/jpeg/png/webp/gif/heic/heif → image; mp4/mov/webm/mkv/avi → video; resto → file. Strip query string + case-insensitive. Log estructurado con count + content_types. (c) `process-debounced.ts` NO se modifica — `history.ts:38-40` ya filtra rows con `content` vacío, así que el media-only inbound salta al skip "no new lead messages" existente. Multimodal IA (transcribir audio Whisper + describir imagen Claude vision) **diferido a bloque dedicado** post-merge. Tests: +7 `parseAttachmentsRaw` + 4 `customData.attachments` + 6 `inferContentTypeFromUrl` = 54 channel-adapters + 123 motor-agente verde + typecheck monorepo verde. **Commit con mensaje genérico "backup: pre-deploy safety checkpoint"** porque un hook auto-backup capturó los cambios antes de que pudiera commitear con mensaje descriptivo. SHA `e9a7864`. **Pendientes Bloque C** (orden ejecución): C.E (captura payload App Marketplace GHL real-time outbound humano — bloqueado hasta que Iván dispare 1 mensaje manual con motor corriendo y comparta el log), C.G smoke "clase" customData, C.H smoke audio/imagen, C.I smoke 3 outbounds (bienvenida humana via app + manual mid-stream pausa IA + self echo no-op), C.J merge a main. **Path A (botón pausa IA Panel SaaS)** queda como failsafe — la pausa primaria viene de la app Marketplace cuando se valide.

- **D58.5 (2026-05-08, sesión continuación) — PR `feat/ghl-as-channel` + C2.2 customData passthrough**. (a) PR abierto manualmente desde URL `https://github.com/ivannsotoo20/setters-ia/compare/main...feat/ghl-as-channel?expand=1` (no hay `gh` CLI en Windows). Body con summary + smoke 1 results + test plan. (b) Commit `33aa5b0` C2.2: parser GHL extiende `workflowSchema` para extraer de `customData` (con fallback top-level): `lead → contactId`, `message → body`, `conversation_source → conversationSource` (bienvenida/lm/inbound/manual, normalizado lowercase+trim), `direction → outbound/inbound`, `messageType → IG/FB Messenger/etc`. Helper `pickConversationSource()` valida enum; valores inválidos retornan `undefined`. `GhlWebhookPayload` + `GhlParsedInbound` extendidos con `conversationSource?` opcional. `parseGhlInboundMessage` propaga el campo. `routeGhlInbound` paso 4.6 hace UPDATE explícito de `conversations.conversation_source` cuando viene en payload (override del default). Tests: +9 nuevos en `ghl-parser.test.ts` cubriendo customData.lead/message/conversation_source/direction/messageType + payload legacy "clase" completo + edge cases (typos rechazados, normalización case+whitespace). 43 tests channel-adapters + 117 motor-agente + typecheck monorepo verde. **Casos de uso desbloqueados**: Iván puede armar Workflow GHL "Si keyword=clase → bienvenida, llama webhook con customData={conversation_source:'bienvenida', lead:{{contact.id}}, message:{{message.body}}}" — el motor lo respeta sin necesitar matching por `automation_keywords`. **Pendientes** (sin cambios respecto D58): smoke 2 Workflow Outbound + pausa IA, automation_keywords reales, merge a main. **Authorization Bearer** como auth alternativa al `:tenant_token` URL queda **deferred** — solo añadir si Iván lo pide explícitamente (con tenant_token URL es suficiente).

- **D56 (2026-05-08, sesión próximos pasos) — Bloque A operacionalizado en código + Bloque B Hito 10 GHL sync implementado + tests**. Plan vigente: `~/.claude/plans/planifica-los-pr-ximos-pasos-fancy-puddle.md`. Cambios: (1) Bloque A: `CREDENTIALS_ENCRYPTION_KEY` + `INTERNAL_STATS_TOKEN` generados via `openssl rand -hex 32` y persistidos en `.env.local`; `YCLOUD_WEBHOOK_VERIFY_MODE=warn` mantenido hasta sincronizar `whsec_` real de YCloud (decisión consciente — saltar a `enforce` con secret placeholder rompería runtime). Backfill `node scripts/encrypt-credentials.mjs` ejecutado: 3/3 `integration_accounts` ahora con `credentials_encrypted` blob `v1:` (ManyChat tenant 2 ×2, YCloud tenant 3 ×1). Plain `credentials` mantenido como fallback durante transición. (2) Bloque B Hito 10 GHL sync: migration `011-ghl-sync-fields.sql` aplicada (conversations.{ghl_contact_id, ghl_opportunity_id, ghl_conversation_id} TEXT nullable + 2 índices parciales); `packages/ghl-client` implementado real (api-client + contacts + messages + opportunities + tipos extendidos + GhlClient orquestador; 27 tests vitest verde) — antes era stub que lanzaba `Error('not implemented')`; helper `apps/motor-agente/src/services/ghl-sync.ts` con 5 funciones best-effort (loadGhlContext, ensureGhlContactAndOpportunity, syncInboundToGhl, syncOutboundToGhl, moveStageForPhase) — todas swallowean errores de GHL API y loggean estructurado, NUNCA rompen pipeline; wireado en `process-debounced.ts` (paso 2.5 ensure + 5.5 inbound + 9.5 stage move) y `outbound-sender.ts:sendNextBatch` (post-INSERT conversation_messages); 14 tests integración mocks vitest verde. **Estado tests**: 8/8 typecheck + 242/242 tests (subió de 201 a 242 con +27 ghl-client + +14 ghl-sync). Working tree con cambios sin commit, esperando smoke real para closing commits B.7. **Pendientes Iván**: (a) copiar `whsec_...` real de panel YCloud → `integration_accounts.webhook_secret` tenant 3 + subir verify mode a `enforce`; (b) crear sub-cuenta "Fyzon Sandbox" GHL Agency Unlimited ($297) + provisionar pipeline F1-F6+F11+F12 + custom fields + copiar `location_id` + `api_token` + ejecutar `INSERT integration_accounts(provider='ghl', tenant_id=3, …)` + correr `node scripts/encrypt-credentials.mjs` post-insert; (c) levantar motor + cloudflared + smoke 5-7 turnos vía WhatsApp real al `+34684703803` para verificar A.4 (`pipeline_runs` + `/internal/stats` + HMAC enforce) y B.6 (contacto + conversation + opportunity + stage moves replicados en panel GHL sandbox). Plan vast-charm.md queda **superseded** por fancy-puddle.md.

- **D55 (2026-05-08) — Coach Iván fuente real de em-dashes + violación R1**. Tras desplegar v4.1, Iván testó otros 4 turnos y las fricciones persistieron. **Diagnóstico**: el coach Iván Soto/Fyzon (`coach_v3` tenant_id=3) contenía **32 em-dashes** en su contenido + frase `"Te entiendo, pero..."` literal en cierre D2 (violando R1 directamente). El modelo aprende del **patrón visual** del coach más que de la regla declarativa cuando hay tanta contaminación. Ejemplos del coach actuaban como entrenamiento implícito contrario a las reglas universales del Cerebro. **Fix**: `sed 's/—/,/g'` en `prompts/source/coach-v3/ivan-dev.md` (25 em-dashes en archivo fuente eliminados; los 32 contados en DB venían de duplicados por contexto), reemplazo D2 `"Te entiendo, pero"` → `"Tiene sentido lo que planteas, pero"`, reemplazo "Cierre post-agenda" `"Perfecto, nos vemos..."` → `"Listo, nos vemos..."` (rompe muletilla repetida). Aplicado vía UPDATE en Supabase 2026-05-08 06:39:29. Verificado: em_dash_count=0, has_te_entiendo=false. **Lección operativa**: cuando una regla universal del Cerebro no surte efecto, sospechar contaminación del Coach del trainer. El Coach enseña por ejemplo, no solo por instrucción. Validar siempre `prompt_blocks WHERE tenant_id=N AND block_key='coach_v3'` antes de iterar el Cerebro.
- **D43–D52 (2026-05-07, REORGANIZACIÓN POST LECTURA COMPLETA TRANSCRIPCIÓN)** — Tras leer la transcripción de la reunión de equipo Fyzon (2.518 líneas, 2026-05-06) entera y el "mini bloque de Rubén" aprobado por el equipo, el plan de 23 secciones del Core v4 queda OBSOLETO. **Nueva arquitectura D43**: Cerebro del Setter (6 sub-bloques universales: Identidad / Propósito / Tareas / Reglas / Objetivos / Resultado esperado) + 6 archivos de fase (D49 con 5 elementos cada una) + 3 protocolos universales (objeciones RAM, descualificación cierre cálido, handoff doble capa) + 1 output_contract. **D44**: renombrar Core → "Cerebro del Setter", Coach → "Información sobre la empresa para la que trabajas". **D45**: F0 (contexto previo bienvenida/lead-magnet/inbound) va al Coach, NO al Cerebro. **D46**: Reglas separadas Críticas (inviolables) + Condicionales (operativas) + Jerarquía de decisión interna. **D47**: 6 reglas absolutas anti-derivación-médica (nunca urgencias/teléfonos/rol salud mental/minimizar/ignorar/alarmar) en Reglas Críticas. **D48**: cualificación general baja al Coach; en el Cerebro solo los 3 criterios universales. **D49**: 5 elementos por fase (Objetivo/Estructura/Resultado esperado/Criterio de avance/Cómo actuar ante imprevistos). **D50**: nueva distribución de fases F1 Conexión+Tema / F2 Contexto+problema / F3 Cualificación 1 pregunta de compromiso / F4 Transición + ¿necesita ayuda? OMITIBLE si verbalizó / F5 Propuesta llamada flexible / F6 Envío enlace + cierre. **D51**: handoff doble capa — Cerebro universal + 6 reglas absolutas / Coach criterios específicos del trainer. **D52**: conectores XML al final, después de cerrar contenido. Plan vigente: `~/.claude/plans/c-users-sotob-downloads-prompt-ejemplo-quirky-puffin.md` (REORGANIZADO 2026-05-07). Backup del archivo firmado original: `prompts/source/core-v4/_legacy/01-role-firmado-2026-05-07.md`. Reciclaje del contenido firmado en sesión previa: ~70% se mantiene literal redistribuido en nueva arquitectura, ~30% se mueve al Coach (todo lo de wording + ejemplos concretos + criterios cualitativos sobre validación).
- Fases estimadas: 19–25 semanas (5–6 meses) desde arranque. Ver sección 6 del plan.
- Ivan todavía NO ha subido al GHL Pro $497. Está en Unlimited $297. Con el $297 basta para MVP (sub-cuentas + modo agencia). Pro se evalúa cuando haya volumen o necesidad de rebranding profundo.

**Al retomar en futura sesión**: leer primero este archivo y luego el plan vigente `~/.claude/plans/planifica-como-podemos-seguir-quiet-wigderson.md`. Estado actual: Hitos 1-8 ✅, trabajo en curso sin commit (YCloud + ivan-dev) en sub-fase Hito 8.5 esperando smoke E2E real con WhatsApp de Ivan. Tras smoke OK → Bloque 1 Hardening (encrypt + HMAC + observabilidad).

## 🔑 Detalles operativos del monorepo (post-Hito 3)

- **Raíz del repo**: `C:\Users\sotob\setters_ia\`
- **Credenciales**: `.env.local` en la raíz del repo (gitignored). Contiene SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ANTHROPIC_API_KEY, REDIS_URL, NEXT_PUBLIC_*. En cada sesión nueva comprobar que existe antes de ejecutar apps.
- **Carga de .env**: el motor usa `dotenv` con `override: true` en `apps/motor-agente/src/config/env.ts`. Motivo: el shell de Ivan (git bash en Windows + entorno Claude Code) tiene `ANTHROPIC_API_KEY=` vacía pre-definida, y `node --env-file` no sobrescribe. `dotenv` con override sí. El panel lee env directamente de Next.js.
- **Comandos base**: `pnpm install`, `pnpm typecheck`, `pnpm build`, `pnpm --filter @fyzon/motor-agente dev`, `pnpm --filter @fyzon/panel dev`, `pnpm db:generate-types` (regenera desde MCP o CLI).
- **Puertos dev**: motor 3001, panel 3000, redis 6379.
- **Docker Desktop**: instalado 2026-04-20 (Docker 29.3.1 vía winget, build 4.68.0). El CLI está en `C:\Program Files\Docker\Docker\resources\bin\docker.exe` — NO en PATH por defecto, así que desde git bash hay que hacer `export PATH="/c/Program Files/Docker/Docker/resources/bin:$PATH"` o invocar con ruta completa. `docker compose up --build` desde la raíz del repo arranca motor + redis; el motor responde correctamente dentro del container. `docker-compose.yml` lee `.env.local` (no `.env`). `.dockerignore` raíz excluye node_modules/dist/.next/.env*. Dockerfile del motor es multi-stage Node 22-alpine con pnpm deploy filtrado.
- **Package versions clave**: Node 22, pnpm 10.33, Next 16.2.4, Turbopack, Tailwind 4, Fastify 5.2, @anthropic-ai/sdk 0.73, @supabase/supabase-js 2.47, TypeScript 5.9 strict.

---

## 📝 EDITAR EL CORE v3 — workflow (crítico, leer antes de tocar el prompt)

El Core v3 es el activo intelectual más valioso del proyecto. Vive en Supabase (`ppujrqxiizgfqclbuxet`, tabla `public.prompt_blocks`, 11 bloques con `tenant_id IS NULL`). **NUNCA se edita directamente en Supabase** — siempre vía el pipeline fuente → script → seed SQL para mantener trazabilidad y reproducibilidad.

### Fuentes canónicas del Core v3

Los archivos markdown de origen son la **única fuente de verdad**:

| Archivo | Contiene |
|---|---|
| `C:\Users\sotob\setters_ia\prompts\source\core-v3\01-plantilla-base-v2.md` | Core base: `<module_hierarchy>`, `<role_and_objective>`, `<mental_model>`, `<identity_and_tone>`, `<core_principles>`, `<message_types>`, `<phase_architecture>`, `<critical_rules>` (11 reglas), `<pre_message_checks>` (9 checks), `<final_instruction>` |
| `C:\Users\sotob\setters_ia\prompts\source\core-v3\02-fases-setting.md` | Fases 1-6 + Bloque 9 Hand-off + Bloque 8 Pipeline GHL + protocolo cualificación. Divisible por headers `# Fase N`, `# Protocolo hand_off`, `# Fases pipeline GHL`, `# Fase cualificacion` |
| `C:\Users\sotob\setters_ia\prompts\source\core-v3\03-bloque-7-ram.md` | Bloque 7 RAM completo (Reconocer + Anotar + Mover, escalado precio por fase, 7+ objeciones canónicas, creencias limitantes, FAQs técnicas) |

### Script generador

`C:\Users\sotob\setters_ia\scripts\build-core-v3-seed.mjs` — toma los 3 archivos, limpia el escape markdown (`\#`, `\*`, `\+`, etc.), divide el archivo de fases por secciones, y genera el SQL con INSERTs usando dollar-quoting (`$FyzonCoreV3Block$...$FyzonCoreV3Block$`) para evitar problemas de comillas.

### Workflow para editar el Core v3 (los 5 pasos)

1. **Editar el archivo fuente correspondiente** en `prompts/source/core-v3/*.md`.
   - Si editas una regla, una fase, un protocolo → siempre en el .md fuente, NUNCA en el SQL generado.
   - Conserva la estructura de headers (`# Fase N`, `# Protocolo hand_off`, etc.) — el script los usa para dividir.
   - Los archivos están en markdown limpio (sin backslashes de escape). Escribe markdown natural.

2. **Regenerar el SQL**:
   ```bash
   cd C:\Users\sotob\setters_ia
   node scripts/build-core-v3-seed.mjs
   ```
   Output esperado: `schema/v1/seeds/002-core-v3-blocks.sql` regenerado, con la tabla de tamaños por bloque en la consola. Si un bloque cambia de tamaño inesperadamente, investiga antes de aplicar.

3. **Revisar el diff** (antes de aplicar):
   ```bash
   git diff schema/v1/seeds/002-core-v3-blocks.sql
   ```
   Confirma que solo cambió lo que esperabas.

4. **Aplicar en Supabase** vía Claude Code local con MCP:
   ```
   Lee el archivo C:\Users\sotob\setters_ia\schema\v1\seeds\002-core-v3-blocks.sql y aplícalo
   en el Supabase conectado vía MCP `supabase-fyzon` usando apply_migration.
   Devuelve el output de la verificación final.
   ```
   El seed es **idempotente**: hace `DELETE ... WHERE version=1` antes del `INSERT`, así que puedes ejecutarlo tantas veces como quieras sin duplicar.

5. **Validar** con el SELECT que devuelve el propio seed: 11 filas, `tenant_id IS NULL`, los `chars` deben coincidir con los que el script reporta en consola.

### Versionado del Core v3 (cuando tocará)

Hoy todos los bloques están en `version=1`. Si un día haces un cambio **breaking** y quieres mantener `v1` accesible para comparar con `v2`:

- Edita el script `build-core-v3-seed.mjs` para que genere inserts con `version=2` en vez de sobrescribir `v1`.
- Marca `is_active=FALSE` en los v1 viejos con un UPDATE manual.
- Deja `is_active=TRUE` solo en los `v2`.
- El motor TS, cuando lo construyamos, leerá los bloques `WHERE is_active=TRUE` → migra automáticamente.

Para cambios menores (typos, afinado de wording) se sobrescribe `v1` sin problema.

### Qué NO es el Core v3 (y por tanto NO va por este flujo)

- **coach_v3** (Directiva del Entrenador por cliente): vive con `tenant_id = <id_trainer>`. Se edita desde el panel del SaaS Fyzon (cuando esté construido) o con scripts custom por trainer, pero NO regenerando `002-core-v3-blocks.sql`.
- **nicho_v3** (Directiva del Nicho, opcional): lo mismo — por trainer o por nicho compartido, pero NO con este pipeline.
- **Bloques específicos de un único trainer**: sort_order > 1000 por convención para distinguirlos del Core compartido.

### Rollback si algo sale mal

Si tras aplicar el seed detectas que el Core se rompió:

1. Revierte el archivo fuente al commit anterior (`git checkout HEAD~1 -- prompts/source/core-v3/01-plantilla-base-v2.md`).
2. Regenera el seed: `node scripts/build-core-v3-seed.mjs`.
3. Aplica de nuevo: idempotencia garantiza que restaura.

Si el pipeline del motor en producción ya empezó a usar el v1 roto y respondió a leads con mensajes raros, revisa `conversation_messages` de los últimos minutos para detectar daño y decide si notificar al trainer.

### Estado actual (2026-04-20)

- ✅ Core v3 cargado en Supabase `ppujrqxiizgfqclbuxet`, versión 1, 11 bloques compartidos (`tenant_id IS NULL`).
- Total 59.167 caracteres distribuidos. core_v3_base=31.458, fases 1-6=17.881, cualificacion=2.355, handoff=1.081, pipeline=788, objeciones=5.604.
- Integridad verificada byte a byte en la aplicación del seed 002.

---

## 🗺️ ROADMAP HASTA FINAL — fases del proyecto SaaS Setters IA

### ✅ Fase 0 — Foundations (en curso, ~80% hecho)

| Hito | Estado | Entregable |
|---|---|---|
| Hito 1: tenant + profile + RLS | ✅ 2026-04-20 | `tenants(id=1, Fyzon dev)` + profile Ivan `owner` + `tenant_configs` con defaults |
| Hito 2: Core v3 cargado | ✅ 2026-04-20 | 11 bloques en `prompt_blocks` con `tenant_id IS NULL`, version=1 |
| Hito 3: Scaffold monorepo TS | ✅ 2026-04-20 | pnpm + Turborepo. 2 apps (motor-agente Fastify + panel Next.js 16 Turbopack) + 6 packages. Docker Compose + Dockerfile motor listos. `curl /health` → `supabase_reachable: true, prompt_blocks_count: 11`. Panel :3000 HTTP 200. |
| Hito 4: Webhook receiver ManyChat + coach Pablo Montenegro | ✅ 2026-04-20 | Tenant 'montefit' (id=2) + coach_v3 (18.961 chars, URL agenda incluida) cargados. Endpoint `POST /webhook/manychat/:tenant_token` con parser Zod (+ sanitizer placeholders IG), dedup Redis, persistencia completa. Contrato ManyChat validado con payload real. 8/8 tests vitest. Token tenant 2: `[REDACTADO — tenant_tokens, tenant 2, purpose=manychat_webhook]`. Channels WA+IG registrados con integration_accounts ManyChat. |
| Hito 5: Prompt composer real | ✅ 2026-04-21 | `packages/prompt-composer`: `composePrompt()` + `buildComposedPrompt()` puro. Ensambla core + coach + fase + condicionales (cualif/handoff/pipeline) + objeciones con cache_control en 2 breakpoints (core y final). CLI `pnpm --filter motor-agente preview-prompt --tenant 2 --phase 2` operativo. 12/12 tests unitarios. Prompt real tenant 2 phase 2: 60.136 chars ≈ 15k tokens, ahorro cache ~90% en inputs tras primera llamada. |
| Hito 6: Generator (Sonnet 4.5) | ✅ 2026-04-26 | `packages/agent-pipeline/runGenerator()`: Anthropic SDK + composePrompt + tool `respond_as_setter` con tool_choice forzado (message_raw, user_summary, conversation_status, phase_decision, resources_to_send, handoff_cause, reasoning). Cost calc Sonnet 4.5 con cache (DEFAULT_PRICE_TABLE). Helper `loadConversationHistory` y `logLlmCall` (best-effort, no-fatal). 21/21 tests vitest (cost + tool schema + validate + 4 generator E2E con mocks). CLI `pnpm --filter motor-agente run-generator --tenant 2 --phase 1 --message "..." --history "a\|user;b\|assistant"`. **Smoke test E2E REAL contra Anthropic OK 2026-04-26**: turno 1 (cache cold) ~$0.085 6.4s, turno 2 (cache parcial) ~$0.047 7.5s. Modelo respeta el coach: "Verga, eso tiene que ser frustrante. Cuando dices que lo dejas a las dos semanas, ¿qué pasa en ese punto?" — patrón "Cuando dices..." obligatorio en F2 aplicado correctamente. |
| Hito 7: Judge + Splitter + Validador V0-V16 + Pipeline orquestador | ✅ 2026-04-26 | **Validador (`@fyzon/shared-validator`)**: 11 reglas reales (V00-V08, V11-V12, V15-V16) + 6 stubs (V09/V10/V13/V14). API `validateMessage(text, ctx, opts)`. 36/36 tests. Severity warn/error. **Judge (Haiku 4.5)** tool `judge_message` (decision pass/fix/reject). System prompt enfocado SOLO en 8 guardrails objetivos (no juzga estilo/vocabulario coloquial — eso es Generator). 5 tests con mock. **Splitter (Haiku 4.5)** tool `split_message` 1-4 partes 20-280 chars + fallback determinístico. Fast path: ≤280 chars no llama modelo. 5 tests. **Pipeline (`runPipeline`)** orquesta Generator→Judge→Validator→Splitter con totals. CLI `pnpm --filter motor-agente run-pipeline ...`. Smoke E2E real: turno completo $0.0145 (Sonnet $0.012 warm + Haiku $0.003), 27.5s. Output natural: "Verga, eso tiene que ser frustrante. ¿Qué pasa en ese punto cuando lo dejas a las dos semanas?". 87 tests totales. |
| Hito 8: Outbound ManyChat + scheduler debounce (loop inbound→outbound cerrado en código) | ✅ 2026-04-26 | **Adapter ManyChat real** (`packages/channel-adapters`): `manyChatSendContent()` POST a `/fb/sending/sendContent` con Bearer auth + `ManyChatApiError` + `ManyChatWhatsAppAdapter` y `ManyChatInstagramAdapter` con `send()` real. 6 tests vitest mock fetch. **Debounce buffer Redis** (`apps/motor-agente/src/lib/debounce-buffer.ts`): sorted set `fyzon:debounce:conversations` con `enqueueDebounce` (extiende score), `getExpiredDebounces`, `dropDebounce`. 8 tests con `ioredis-mock`. **Scheduler** (`scheduler.ts`): `computeScheduledTimes` (active_delay 30s + 10s entre partes), `intervalToSeconds` (HH:MM:SS o ISO PT), `nextRetryAt` con backoffs [5s, 30s, 5min], `insertScheduledParts` / `markScheduleSent` / `markScheduleFailed` / `rescheduleForRetry`. 9 tests. **Servicios**: `processDebounced` (carga conversación + history, separa últimos inbounds del lead, ejecuta `runPipeline`, programa partes, actualiza `conversations.phase_number/state`); `sendNextBatch` (lock por status='processing', envía vía adapter por canal, actualiza estados, replica a `conversation_messages` source='ai'). **Plugin Fastify** `cron-scheduler.ts`: 2 setIntervals (debounce-tick 5s, outbound-tick 5s), arranca en `onReady`, se cancela en `onClose`. **Wiring**: webhook llama `enqueueDebounce` tras `insertInboundMessage`; `MANYCHAT_API_BASE` añadido a env. **CLIs**: `pnpm --filter motor-agente run-debounce-tick [--conversation N]` y `run-outbound-tick [--batch N]` para forzar iteraciones manuales. **Tests totales: 110/110** (12 composer + 31 pipeline + 36 validator + 6 channel-adapters + 25 motor-agente). Smoke E2E real (Ivan envía IG real) queda como ⏳ **paso final manual de verificación** (ngrok + ManyChat flow + WhatsApp). |
| **Fase 1 cerrada en código** — listo para "moment of truth" E2E real | ⏳ | Smoke completo: Ivan envía IG real, motor responde realmente. Tras eso, Fase 1 ✅ y arranca Fase 2 (panel SaaS). |
| Hito 8.5 — Multi-provider WhatsApp (YCloud) + tenant `ivan-dev` sandbox | 🔨 código completo, smoke pendiente | **Adapter YCloud completo** (`packages/channel-adapters/src/ycloud/{api-client,types,parser,whatsapp}.ts`): POST `/v2/whatsapp/messages/sendDirectly` con header `X-API-Key`, parser de payload Meta-style, dedup por `wamid`. **Multi-provider routing** en `apps/motor-agente/src/services/outbound-sender.ts:buildAdapter()` y `normalizeProvider()` — soporta `manychat` y `ycloud`. **Webhook nuevo** `apps/motor-agente/src/routes/webhook-ycloud.ts`. **Migration `005-ycloud-provider-enum.sql`** aplicada (enum `channel_provider` ahora incluye `ycloud`). **Seeds 005+006**: tenant `ivan-dev` (id=3) "Ivan / Fyzon Sandbox" + canal WA via YCloud (channel_id=4) + integration_account con API key real (32 chars hex) + business_phone `+34684703803` + token webhook `[REDACTADO — tenant_tokens, tenant 3, purpose=ycloud_webhook]` + coach_v3 real (18.808 chars, Iván Soto / Fyzon). Tests **125/125 verde** (subió de 110 con +7 parser-ycloud + +8 ycloud-api). Decisiones D30/D31/D32. **Pendiente**: smoke E2E real con WA — Ivan arranca Docker Desktop + cloudflared + configura webhook en panel YCloud + envía mensaje WA real al `+34684703803`. |
| Hito 9 — Hardening pre-producción (encrypt + HMAC + observabilidad) | ⏳ siguiente bloque | Tras smoke 8.5 OK. **1.1 AES-256-GCM credentials**: `apps/motor-agente/src/lib/crypto.ts` + migration 006 + script `scripts/encrypt-credentials.mjs` + reemplazar lecturas plain en `outbound-sender.ts:loadSendContext`. **1.2 HMAC verification**: `webhook-verify.ts` + raw-body Fastify + secret per integration_account (YCloud lo soporta; ManyChat se documenta como deuda). **1.3 Observabilidad**: correlation_id por inbound, tabla `pipeline_runs`, endpoint `/internal/stats` con bearer token. Migrations 006/007/008. Decision D33. Plan: `~/.claude/plans/planifica-como-podemos-seguir-quiet-wigderson.md`. |

### 🔨 Fase 1 — Motor del agente + ManyChat (3-4 semanas estimadas)

Objetivo: pipeline completo inbound → Claude → outbound funcionando para el tenant de prueba (Fyzon dev).

- Webhook receiver `/webhook/manychat/:tenant_token` en Fastify (apps/motor-agente)
- Parser/normalizer de payload ManyChat (WA + IG + FB) con detección de audio/imagen vía `lookaside`
- Transcripción automática (Haiku 4.5 audio + Gemini Flash vision imagen) → pasa texto al pipeline
- **Pipeline 3 LLMs con Claude** (packages/agent-pipeline):
  - Generator (Sonnet 4.5 con prompt caching) → message_raw + user + conversation_status + resources
  - Judge Validator (Haiku 4.5) → reescribe si viola guardrails
  - Message Splitter (Haiku 4.5) → divide en 1-4 mensajes naturales 20-280 chars
- **Validador V0-V16** (packages/shared-validator) portado del JS del n8n a TS puro
- Composer de prompt (packages/prompt-composer): ensambla `core_v3_base` + fase activa + `coach_v3` + `nicho_v3` + validator/critical_rules + historial + mensaje actual, con prompt_caching por capa
- Message scheduler con typing delay (active_delay | idle_delay + position*10s) → INSERT `message_schedules`
- Cron que lee `message_schedules` pending y envía vía ManyChat API (packages/channel-adapters)
- Sync GHL: cada mensaje (inbound + outbound) se replica a `POST /conversations/messages` type='WhatsApp' o 'Custom'
- Deduplicación: UNIQUE constraint en Redis buffer por (integration_account_id, external_user_id, timestamp_ms)
- HMAC verification del webhook con `webhook_secret` de `integration_accounts`
- Tests de regresión: fixtures de las 3 conversaciones 10/10 (C1 difícil, C2 cualificable, C3 no cualifica) — el motor debe alcanzar mismo handoff + cumplir 11 critical_rules

### 🎨 Fase 2 — SaaS Panel v1 (3-4 semanas estimadas)

Objetivo: el trainer se da de alta, configura su setter sin tocar código, y ve las conversaciones.

- Next.js 16 + Tailwind 4 + shadcn/ui (apps/panel)
- Auth Supabase con magic link + OAuth Google
- **Onboarding wizard 3 pasos (plug-and-play ManyChat)**:
  1. Trainer pega API key ManyChat → Fyzon valida llamando a `/fb/page/getInfo` → guarda encriptada en `integration_accounts.credentials`
  2. Trainer importa plantilla ManyChat pública preconfigurada con HTTP Request hacia `/webhook/manychat/{tenant_token}`
  3. Trainer asigna sus keywords/triggers
- **Configurador de coach_v3** (el "compilador"):
  - Formulario estructurado → genera markdown del Bloque 2 → inserta en `prompt_blocks` con `tenant_id=<trainer>`
  - Secciones: Identidad (nombre/bio/foto), Nicho (preset + custom), Programa (pilares/precios/testimonios), Criterios cualificación/descualificación, Tono (slider + emojis whitelist + apelativos), Banco ≥20 frases reales, Mensajes obligatorios por fase, Afectaciones estructura
  - Validador de completitud: fuerza ≥20 frases antes de activar
  - Moderación del bloque libre con Haiku antes de persistir
- Visor conversaciones realtime (Supabase Realtime)
- Dashboard métricas con los 3 KPIs Rubén (respuesta bienvenida / respuesta primera pregunta / **% links enviados = KPI REY, target >15%**)
- Test button end-to-end (simula mensaje entrante y verifica pipeline)
- Gestión de recursos: subir PDFs/videos a Supabase Storage + reglas de envío
- Config de delays, horarios, blacklist (`ignored_users`)
- Versionado de coach_v3 (draft / active / histórico) con rollback

### 💳 Fase 3 — GHL backbone + Stripe + onboarding BSP ManyChat (2-3 semanas)

- Sub-cuenta GHL automática por trainer vía GHL Agency Unlimited $297 (API Lead Connector → create location)
- Pipeline "Gestión de Leads" con stages F1-F6 + F11 Espera + F12 Perdido aprovisionado automáticamente por cada trainer nuevo
- Custom fields GHL: `contact.external_id`, `contact.username`, `contact.conversation_context`, `contact.lead_goal`, `contact.primary_problem`
- Stripe billing: setup fee + suscripción mensual. Webhooks Stripe → `subscriptions` table
- Módulo follow-ups automatizados (tabla `follow_ups` + cron programa mensajes en `message_schedules` con `message_type='follow_up'`)
- Documentación de onboarding para trainer (vídeos 2 min por paso)

### 🚀 Fase 4 — Migración controlada (2-3 semanas)

- Trainer piloto 1 (Fyzon dev / Ivan) — uno a uno, validar en real con leads pequeños
- Trainer piloto 2-3 — añadir, supervisión humana, comparativa de KPIs vs setter n8n actual
- Ajustes iterativos del Core v3 y del pipeline según feedback
- **Decision gate**: si KPI-3 del SaaS ≥ KPI-3 del n8n para los 3 pilotos → aprobar escalado a más trainers
- Documentación de soporte + playbook de incidencias

### 🌍 Fase 5 — Apertura a nuevos trainers (continuo)

- Landing del SaaS Fyzon con copy convincente
- Self-service onboarding completo sin intervención humana
- Escalado de infra según demanda (upgrade VPS Contabo o migración a Fly.io/Render si hiciera falta)
- Monitorización de costes API Claude por tenant (alertas si se dispara el uso)
- Cohorte por cohorte, abrir a los ~44 trainers actuales del Supabase viejo (uno a uno reonboarding manual por Ivan)
- KPIs objetivo a los 6 meses de apertura: 100+ trainers activos, churn <5% mensual

### 🔮 Fase 6 — Post-MVP (futuro)

- Agenda nativa (OAuth Google Calendar + GHL Calendar directo, no enlace externo)
- Edición avanzada de fases por el trainer (hoy solo toggle, después editar contenido)
- Respuestas rápidas / FAQ personalizadas
- **Migración a Meta Cloud API** (cuando Ivan sea BSP aprobado 3-6 meses desde la solicitud): adapter swap en `channel-adapters` sin tocar el resto del motor
- Dashboard avanzado: predictive insights (probabilidad de cierre del lead, recomendación de follow-up)
- Multi-coach por tenant (varios setters por trainer, para equipos)

---

## 🔄 CÓMO ARRANCAR LA PRÓXIMA SESIÓN

**Trigger**: Ivan dirá algo como *"retomamos setters IA, vamos con el Hito 3"* o simplemente *"seguimos con el scaffold"*.

**Lo primero que tiene que hacer Claude al retomar**:

1. Leer este archivo de memoria (ya se carga automáticamente al abrir el proyecto `C--Users-sotob-setters-ia`)
2. Leer el plan maestro completo: `C:\Users\sotob\.claude\plans\lo-que-te-voy-shimmering-toucan.md`
3. Confirmar con Ivan el estado antes de tocar código: "Retomamos. Estado: Hitos 1 y 2 ✅ cerrados. Arranco el Hito 3 (scaffold monorepo TS). ¿Confirmas?"
4. Tras confirmación, empezar el scaffold:
   - `pnpm init` en la raíz + `pnpm-workspace.yaml`
   - `turbo.json`
   - `apps/motor-agente` (Fastify + TypeScript strict + Anthropic SDK + cliente Supabase con service_role)
   - `apps/panel` (Next.js 16 + Tailwind 4 + shadcn/ui + Supabase Auth)
   - `packages/db` (tipos TS generados del MCP Supabase con `generate_typescript_types` + Prisma schema si decidimos añadir)
   - `packages/shared-validator` (esqueleto V0-V16)
   - `packages/channel-adapters` (esqueleto ManyChatWhatsAppAdapter + ManyChatInstagramAdapter)
   - `packages/agent-pipeline` (esqueleto Generator + Judge + Splitter)
   - `packages/prompt-composer` (función que ensambla bloques desde `prompt_blocks`)
   - `packages/ghl-client` (esqueleto cliente GHL REST)
   - Scripts setup `.env` con SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY + ANTHROPIC_API_KEY (BYO en runtime)
   - Docker Compose para motor + Redis (BullMQ o debounce buffer)
   - CLAUDE.md del proyecto con instrucciones para Claude
5. No asumir — preguntar a Ivan las decisiones abiertas (Node 20 vs 22, turbopack vs webpack, Prisma vs query builder, deploy Fly.io vs Vercel+Contabo, etc.) con AskUserQuestion antes de pinear en `package.json`.

**NO repetir**: todo lo del Discovery (decisiones D1-D18, análisis de flows, auditoría Supabase viejo, etc.) ya está cerrado y documentado en el plan. La próxima sesión arranca con código, no con re-explicaciones.
