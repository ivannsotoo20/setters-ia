{
  "name": "@fyzon/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tipos TS del schema Supabase + helpers de queries tipadas para el motor y el panel",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.generated.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not configured yet'",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.12"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
