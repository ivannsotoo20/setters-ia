{
  "name": "@fyzon/agent-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pipeline 3 LLMs: Generator (Sonnet 4.5) + Judge (Haiku 4.5) + Splitter (Haiku 4.5)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not configured yet'",
    "test": "vitest run",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.73.0",
    "@fyzon/prompt-composer": "workspace:*",
    "@fyzon/shared-validator": "workspace:*",
    "@supabase/supabase-js": "^2.47.12"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.9"
  }
}
